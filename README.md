# Lambda Create Products

This function will create a new product on DynamoDB

# Configuration

```bash
# create an env file
$ cp .env.example > .env
```

# Installation

```bash
# install all dependencies
$ npm i
```

# Deploy

```bash
$ npm run deploy
```
